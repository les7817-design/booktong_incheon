{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "$changes = git status --porcelain; if ($changes) { git add -A; $msg = \"Auto-update: $(Get-Date -Format 'yyyy-MM-dd HH:mm')\"; git commit -m $msg; git push origin HEAD } ",
            "statusMessage": "GitHub에 자동 반영 중...",
            "timeout": 60
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(git *)"
    ]
  }
}
